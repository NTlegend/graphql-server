type Mutation {
    addComment (post_id: Int!, message: String!): Comment
}

type Comment {
    id: Int!
    message: String!
    created_at: String!
    author: User!
    likes: [Like]
    comments: [Comment]
}
